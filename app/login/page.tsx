'use client';
import { FC } from 'react';
import LoginForm from '@/components/forms/login/LoginForm';

const Login: FC = () => {
  return (
    <section>
      <LoginForm />
    </section>
  );
};

export default Login;
