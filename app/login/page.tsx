import { FC } from 'react';
import LoginForm from '@/components/forms/login/LoginForm';

const LoginPage: FC = () => {
  return (
    <section>
      <LoginForm />
    </section>
  );
};

export default LoginPage;
