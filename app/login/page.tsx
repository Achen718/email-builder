'use client';
import { FC } from 'react';
import LoginForm from '@/components/forms/Login';

const Login: FC = () => {
  return (
    <section>
      <LoginForm />
    </section>
  );
};

export default Login;
