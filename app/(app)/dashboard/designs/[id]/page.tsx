interface Params {
  id: string;
}

const DesignsIdPage = ({ params }: { params: Params }) => {
  return <>Designs Id Page, lists all Designs based on current user</>;
};

export default DesignsIdPage;
